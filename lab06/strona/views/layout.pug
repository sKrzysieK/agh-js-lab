doctype html
html(lang='pl')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title= title
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css', rel='stylesheet')
    style.
      html, body {
        height: 100%;
      }
      .wrapper {
        min-height: 100%;
        display: flex;
        flex-direction: column;
      }
      .content {
        flex: 1;
      }
  body
    .wrapper
      nav.navbar.navbar-expand-lg.navbar-light.bg-light
        .container-fluid
          a.navbar-brand(href='#')
            canvas#canvas(width='130', height='50')
              | Wygląda na to, że twoja przeglądarka nie obsługuje elementu "canvas"
          button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#navbarScroll', aria-controls='navbarScroll', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
          #navbarScroll.collapse.navbar-collapse
            ul.navbar-nav.me-auto.my-2.my-lg-0.navbar-nav-scroll(style='--bs-scroll-height: 100px')
              li.nav-item.dropdown
                a.nav-link.dropdown-toggle(href='#', id='navbarScrollingDropdown', role='button', data-bs-toggle='dropdown', aria-expanded='false')
                  | Książki
                ul.dropdown-menu(aria-labelledby='navbarScrollingDropdown')
                  li
                    a.dropdown-item(href='#') Do wypożyczenia
                  li
                    a.dropdown-item(href='#') Do kupienia
            form.d-flex
              input.form-control.me-2(type='search', placeholder='Search', aria-label='Search')
              button.btn.btn-outline-success(type='submit') Search
      .content
        block content
      footer.footer.text-center.text-lg-start.bg-body-tertiary.text-muted
        section.d-flex
          .container.text-center.text-md-start.mt-5
            .row.mt-3
              .col-md-4.col-lg-3.col-xl-3.mx-auto.mb-4
                p Biblioteka Głowna Akademii Górniczo-Hutniczej
              .col-md-4.col-lg-3.col-xl-3.mx-auto.mb-4
                p
                  i.fas.fa-envelope.me-3
                  | bgagh@bg.agh.edu.pl
              .col-md-4.col-lg-3.col-xl-3.mx-auto.mb-md-0.mb-4
                p
                  i.fas.fa-phone.me-3
                  | + 01 234 567 88
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js')
    script.
      "use strict";
      const canvas = document.getElementById("canvas");
      const ctx = canvas.getContext("2d");
      ctx.scale(1 / 2, 1 / 2);

      ctx.beginPath();
      ctx.beginPath();
      ctx.arc(50, 105, 100, (Math.PI * 4) / 3 + 0.4, (Math.PI * 7) / 4);
      ctx.lineWidth = 5;
      ctx.stroke();

      ctx.arc(200, 105, 100, -(Math.PI * 3) / 4, -Math.PI / 3 - 0.4);
      ctx.lineWidth = 5;
      ctx.stroke();

      ctx.beginPath();
      ctx.arc(50, 120, 100, (Math.PI * 4) / 3 + 0.2, (Math.PI * 7) / 4);
      ctx.lineWidth = 7.5;
      ctx.stroke();

      ctx.arc(200, 120, 100, -(Math.PI * 3) / 4, -Math.PI / 3 - 0.2);
      ctx.lineWidth = 7.5;
      ctx.stroke();

      ctx.beginPath();
      ctx.arc(50, 140, 100, (Math.PI * 4) / 3, (Math.PI * 7) / 4);
      ctx.lineWidth = 10;
      ctx.stroke();

      ctx.arc(200, 140, 100, -(Math.PI * 3) / 4, -Math.PI / 3);
      ctx.lineWidth = 10;
      ctx.stroke();

      ctx.beginPath();
      ctx.lineWidth = 4;
      ctx.moveTo(40, 85);
      ctx.lineTo(220, 85);
      ctx.stroke();
